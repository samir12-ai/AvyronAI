FIX PLAN PERSISTENCE + RUN A FRESH REAL CAMPAIGN + VERIFY THE PREVIEW SHOWS THE EXACT LATEST RUN

We have TWO objectives in this task.

Do not expand scope beyond them.

OBJECTIVE 1:
Fix the confirmed strategic plan persistence bug.

OBJECTIVE 2:
Run a completely fresh real MarketMindAI pipeline and prove that the UI/Preview I see is displaying the outputs and snapshots from THAT EXACT NEW RUN — not an older run, stale snapshot, cached result, or fallback plan.

Campaign:

campaign_1773576062201_6t0oxi

IMPORTANT:

Do NOT reopen confidence architecture.
Do NOT reopen CEL architecture.
Do NOT modify Business Profile.
Do NOT work on Performance Loop yet.
Do NOT fabricate data.
Do NOT manually insert a fake plan.
Do NOT manually edit plan status.
Do NOT force approval.
Do NOT use old snapshots to make the UI look complete.

===========================================================
PART 1 — FIX THE PLAN PERSISTENCE BUG
===========================================================

First reproduce the confirmed bug.

The latest successful real run reached:

15/15 engines
→ CEL PASS
→ System Control: zero BLOCK codes
→ Build Plan: SUCCESS

But its generated plan was never persisted into:

strategic_plans

The real-run path ended with:

orchestrator_jobs.status = PARTIAL
orchestrator_jobs.plan_json = null

and later `run-real-campaign.ts` fell back to the older July 31 strategic plan.

That behavior is incorrect.

Trace:

normal orchestrator path
vs
run-real-campaign.ts path

from:

engine completion
→ System Control
→ Build Plan
→ plan finalization
→ plan persistence
→ strategic_plans

Prove the exact divergence before changing code.

===========================================================
CORRECT LIFECYCLE
===========================================================

The intended lifecycle is:

BLOCK
→ do not create an approvable plan

PASS
→ Build Plan
→ persist strategic plan

DOWNGRADE / review_required
→ Build Plan
→ persist strategic plan
→ preserve DOWNGRADE/review metadata
→ wait for human approval

DOWNGRADE must NOT throw the generated plan away.

===========================================================
USE ONE CANONICAL PERSISTENCE PATH
===========================================================

Do not solve this by adding a random direct DB insert only inside the test script.

Inspect whether the normal orchestrator already has canonical plan finalization/persistence logic.

Prefer sharing/reusing that logic so:

normal production run
and
real campaign verification run

cannot produce different persistence behavior.

The persisted plan must preserve correct:

accountId
campaignId
jobId/run lineage
rootBundleId
plan version
planSource
System Control verdict
review_required metadata
engine lineage
snapshot lineage

No stale fallback is allowed.

===========================================================
REMOVE THE DANGEROUS FALLBACK
===========================================================

Audit `run-real-campaign.ts` behavior where it searches for the current run's plan and then falls back to:

latest strategic plan by campaignId

This previously caused the July 31 degraded plan to appear as if it belonged to the August 8 run.

That must never happen.

For verification artifacts:

if the exact current run has no persisted plan:

FAIL CLOSED.

Report:

CURRENT_RUN_PLAN_NOT_PERSISTED

Do NOT substitute an older plan.

===========================================================
PART 2 — RUN A COMPLETELY FRESH REAL CAMPAIGN
===========================================================

After the persistence fix and regression tests pass:

Run ONE fresh MarketMindAI campaign through the real production pipeline.

No synthetic data.
No fake snapshots.
No manual event insertion.
No manual plan insertion.
No cached plan substitution.
No forced PASS.

Capture the exact:

runId
jobId
accountId
campaignId
start timestamp

Then let all engines execute normally.

Verify:

- 15/15 engine execution
- exact engine statuses
- exact snapshots created by this run
- CEL
- grounding
- System Control
- Build Plan
- plan persistence

===========================================================
PART 3 — SNAPSHOT LINEAGE MUST BE EXACT
===========================================================

This is CRITICAL.

For every engine output generated by the new run, identify:

engineId
runId
snapshotId
snapshotVersion
createdAt
campaignId
accountId

Prove that the snapshots belong to THIS exact run.

Do not accept:

"latest snapshot for campaign"

as sufficient proof if a run-specific snapshot reference exists.

I want exact lineage:

NEW RUN
→ ENGINE EXECUTION
→ NEW SNAPSHOT
→ PERSISTED SNAPSHOT ID
→ API RESPONSE
→ UI/PREVIEW

===========================================================
PART 4 — AUDIT THE PREVIEW / FRONTEND READ PATH
===========================================================

After the fresh run finishes, inspect the UI/Preview I use to view the campaign results.

Find every API/query/hook involved in showing:

- Market Intelligence
- Audience
- Positioning
- Differentiation
- Mechanism
- Offer
- Awareness
- Funnel
- Persuasion
- Integrity
- Statistical Validation
- Budget Governor
- Channel Selection
- Iteration
- Retention
- Strategic Plan

Trace:

UI component
→ hook
→ API
→ backend query
→ snapshot/plan selection

For every section determine how it chooses the record:

EXACT_RUN
LATEST_BY_CAMPAIGN
LATEST_BY_ENGINE
CACHED
FALLBACK
LEGACY
UNKNOWN

===========================================================
PART 5 — THE PREVIEW MUST SHOW THE NEW RUN
===========================================================

After the new run completes, verify the actual Preview/UI.

The result displayed in front of me must correspond to the fresh run we just executed.

For every major section verify:

Displayed engine result
=
snapshot generated by NEW_RUN_ID

The Strategic Plan displayed must satisfy:

displayedPlan.jobId/run lineage
=
NEW_RUN job lineage

Do NOT consider the test successful merely because the UI contains plausible-looking data.

The IDs must match.

===========================================================
PART 6 — NO STALE SNAPSHOT MIXING
===========================================================

Specifically test for this failure mode:

MI from new run
+
Audience from old run
+
Positioning from new run
+
Offer from old run
+
old Strategic Plan

This must NOT be presented as one coherent current analysis.

Check every preview query for:

ORDER BY created_at DESC
LIMIT 1

or similar campaign-wide "latest" behavior.

Determine whether it can mix engine outputs from different orchestrator runs.

If exact run lineage is available, the Preview should use the coherent snapshot set belonging to the selected/latest completed run.

Fix proven stale/mixed-run display bugs.

Do not redesign the UI.

===========================================================
PART 7 — REFRESH / CACHE BEHAVIOR
===========================================================

Verify what happens after the new run finishes.

Check:

- React Query cache keys
- invalidation
- refetch
- staleTime
- backend caching
- snapshot API caching
- plan caching
- local state
- persisted frontend state

The user should not need to restart the entire application to see the new analysis.

After a successful run:

the Preview must refetch/invalidate the relevant campaign analysis data.

If a stale cache bug exists, fix it.

===========================================================
PART 8 — VISUAL PROOF IN THE PREVIEW
===========================================================

Open the actual Preview after the run.

Verify visually that the newest results are rendered.

Where technically available, compare visible values against the new snapshot contents.

Examples:

Positioning territory shown in UI
=
territory in new Positioning snapshot

Offer shown in UI
=
new Offer snapshot

Audience shown in UI
=
new Audience snapshot

Strategic Plan shown in UI
=
new persisted strategic plan

Do not rely only on backend tests.

I want frontend runtime verification too.

===========================================================
PART 9 — REGRESSION TESTS
===========================================================

Add tests proving:

1. PASS run persists its plan.

2. DOWNGRADE/review_required run persists its plan.

3. BLOCK does not create an approvable plan.

4. Real-run verification cannot fall back to an older campaign plan.

5. Current run plan must match current job/run lineage.

6. Preview cannot silently substitute an old strategic plan.

7. Snapshot selection cannot mix unrelated runs when coherent run lineage exists.

8. Fresh run invalidates/refetches the relevant Preview data.

9. Existing tenant isolation remains intact.

10. No fake data is created.

===========================================================
PART 10 — DO NOT APPROVE YET
===========================================================

For this task, STOP after proving:

Fresh Real Run
→ snapshots persisted
→ strategic plan persisted
→ Preview displays exact fresh-run results

Do NOT approve the plan yet.

Do NOT activate it yet.

I want to personally inspect the fresh results in the Preview first.

===========================================================
FINAL REPORT
===========================================================

Give me:

1. Root cause of the plan persistence bug.
2. Exact fix.
3. Files changed.
4. Fresh run ID.
5. Fresh job ID.
6. 15 engine results.
7. System Control verdict.
8. Build Plan result.
9. New strategic plan ID.
10. Proof plan belongs to the fresh run.
11. Snapshot IDs created by the fresh run.
12. Preview API/read path.
13. Any stale/mixed-run bugs discovered.
14. Cache/refetch bugs discovered.
15. Fixes applied to Preview data selection.
16. Regression test results.
17. TypeScript baseline vs after.
18. Proof no fake data was created.

Most importantly answer:

A. Did the new run produce and persist a NEW strategic plan?

B. Does that plan belong to the exact new run?

C. Is the Preview currently showing the exact new run's engine snapshots?

D. Is the Preview currently showing the exact new strategic plan?

E. Is any section still showing data from an older run?

F. Can I now open the Preview and personally inspect the newest complete analysis?

STOP there.

Do not approve.
Do not activate.
Do not start Performance Loop yet.