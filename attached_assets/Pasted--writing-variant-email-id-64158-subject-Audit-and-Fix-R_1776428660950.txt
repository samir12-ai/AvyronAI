:::writing{variant=“email” id=“64158” subject=“Audit and Fix Required — Build Plan Re-run Must Save New Snapshot Data to Each Engine Section”}
Hi,

We need a focused audit on the Build Plan → Re-run Pipeline flow.

This is a strict data-consistency issue.

⸻

Objective

When a user opens Build Plan and runs a new pipeline analysis:

every engine section in the dashboard must display the newly generated analysis from the latest run, not old snapshot data.

If a new Market Intelligence analysis is generated from Build Plan, then when the user opens the Market Intelligence section, it must show the new Market Intelligence snapshot from that re-run, not an older one.

The same rule applies to every engine section.

⸻

What to Audit

Please trace the full flow for:
	•	Build Plan → Re-run Pipeline
	•	engine execution
	•	snapshot creation
	•	snapshot persistence
	•	section-level reads in the dashboard

Audit whether each engine section is actually reading the snapshot created by the latest Build Plan run.

⸻

Required Checks

For each engine section, verify:
	1.	When a new analysis is generated, is a new snapshot row actually created?
	2.	Is that new snapshot stamped with the correct:
	•	jobId
	•	campaignId
	•	accountId
	•	createdAt
	3.	When the user opens the engine section, does the section read:
	•	the snapshot from the latest Build Plan run
	•	or an older snapshot
	•	or latest-by-table without run binding
	4.	Are there any places where the section UI still falls back to:
	•	older engine data
	•	latest snapshot by campaign
	•	cached pre-run analysis
	5.	Does each engine section use the same runId/jobId as the Build Plan run that the user just triggered?

⸻

Engines to Verify

Audit all engine sections, including at minimum:
	•	Market Intelligence
	•	Audience
	•	Positioning
	•	Differentiation
	•	Mechanism
	•	Offer
	•	Awareness
	•	Funnel
	•	Persuasion
	•	Integrity
	•	Statistical Validation
	•	Budget Governor
	•	Channel Selection
	•	Iteration
	•	Retention

⸻

Expected Correct Behavior

After a Build Plan re-run:
	•	each engine section must show the analysis generated by that run
	•	no section should show an old snapshot
	•	no section should mix old and new analysis
	•	no section should silently fall back to previous data unless clearly marked stale

⸻

If Problems Exist

If any section is still reading old data:
	•	identify the exact code path
	•	identify whether the problem is:
	•	snapshot not being saved
	•	snapshot saved but not read
	•	section endpoint not run-bound
	•	UI not passing runId
	•	cache not invalidated
	•	fix it at the root

Do not patch only the UI label.
Do not hide the issue with stale banners.
Fix the actual save/read binding.

⸻

Required Output

Return:

1. Audit Findings

For each engine section:
	•	does it save new snapshot correctly? yes/no
	•	does it read the new snapshot correctly? yes/no
	•	any mismatch? yes/no

2. Root Causes

For each mismatch:
	•	exact file
	•	exact function
	•	exact cause

3. Fixes Applied
	•	what was changed
	•	why it solves the issue

4. Validation Proof

Trigger a Build Plan re-run and prove that:
	•	a new snapshot is created for each engine
	•	the engine section displays the new snapshot
	•	old snapshot data is not shown after re-run

⸻

Important

This is specifically about:

Build Plan re-run → engine section snapshot truth

We need to guarantee that a fresh analysis really becomes the visible analysis in each engine section.

Proceed with the audit and fix any issue you find.

Thanks.
:::