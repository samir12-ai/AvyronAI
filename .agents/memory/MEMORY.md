- [Product Identity source-of-truth](product-identity-sot.md) — product_anchor = Product Identity (preferred anchor); business_data_layer = supplementary context (not replaceable yet — 4 fields have no product_anchor equivalent); content_dna = competitor MI (unrelated).
- [React Query v5 polling signature](react-query-v5-polling.md) — v4-style `(data) =>` refetchInterval callbacks silently never poll under v5 (`query.state.data`); UI freezes on in-progress states while the backend row is already terminal — check DB final state first.
- [Avyron verification & tsc workflow](avyron-verification-workflow.md) — run tsc alone (chained greps time out & truncate it); judge by net-new errors vs the large baseline; verify orchestrator behaviour with focused tsx harnesses driving the real units, not a full DB+proxy run.
- [Avyron deploy/boot failure modes](avyron-deploy-boot.md) — esbuild bundles unimported identifiers (crash at boot, not build); global /api authMiddleware gate means no per-route auth; prod is verify-only so prod DB must be migrated to REQUIRED_SCHEMA_VERSION independently.
- [DrizzleQueryError .cause shape](drizzle-error-cause-shape.md) — pg detail lives in e.cause.message not e.message; catch/idempotency guards matching e.message silently break after a Drizzle bump.
- [Positioning gate coherence](positioning-gate-coherence.md) — composer prompt, grounding allowlist, and judge anchor must change in lockstep or the engine doom-loops; judge rejection with generic DNA is truthful degradation, fix via richer DNA not gate tuning.
- [MI snapshot resolution](mi-snapshot-resolution.md) — MI snapshots rarely carry orch runIds; run-pinned lookups starve reuse_allowed surfaces. Phantom drizzle column ⇒ "Cannot convert undefined or null to object".
- [Persisted UI-state hydration race](persisted-state-hydration-race.md) — async server hydration resets local UI state and clobbers mount-time deep links; gate the reset behind an unconsumed-intent ref and e2e-test the cold path.
- [Apify acquisition layer](apify-acquisition.md) — profile actors return NO comments (dedicated comment actor per platform); every provider-error catch must force TRANSIENT + sanitize 403/429 or false 24h platform blocks get stamped; runId null = healthy budget-filter empty.
- [Retry paths drop context threading](retry-path-context-threading.md) — regenerate/retry call sites silently omit args the attempt-1 call passes; audit every retry branch and verify per-attempt evidence logs.
- [Scrape freshness gates come in pairs](scrape-freshness-gates.md) — outer campaign gate + inner per-profile gate; a status filter (exclude FAILED) applied to only one silently suppresses retries.
- [Shared-table row classes](shared-table-row-classes.md) — new row class in a shared table: fence EVERY reader; open-state unique indexes need expiry closure; two-fetch confirmation diffs vs stored baseline.
- [performance_snapshots row shapes](performance-snapshots-checkpoint-rows.md) — sync rows carry economics, revisit checkpoint rows are explicit-null; readers must filter by checkpoint or fabricate zeros; dedupe = DB partial unique index.
- [Engine result field-drift](engine-field-drift.md) — consumer reads of fields engines never emit yield undefined → gates see 0/empty and fail truthfully on healthy content; dump the actual evaluated input first, fix the read path never the gate.
- [_provenance key ownership](provenance-key-ownership.md) — engine-result `_provenance` belongs to the snapshot-reuse trust layer only; other metadata needs its own key or fresh outputs classify as REUSED_UNVERIFIED.
- [Funnel depth gate vs grounding](funnel-depth-gate-vs-grounding.md) — GROUNDING_CONTRACT_MET ≠ depth PASS: gate scores the deterministic funnel structure (no AEL refs), not the LLM's grounded output; verify per-attempt scored input empirically before trusting a scoring-input diagnosis.
- [Grounding contract verification](grounding-contract-verification.md) — groundingRefs are logged not persisted; ael=null ⇒ silent MET; grep -c over run log over-counts markers (use SUMMARY.json markerCounts).
- [DNA Enrichment Gate behaviour](dna-enrichment-gate-behavior.md) — Path A candidate re-passes the UNCHANGED judge (truthful reject is correct) → Path B flag; judge by the gate loop closing, not engine count — a richer anchor can reach FEWER engines yet still be BLOCKED.
- [Fetch completeness ⇄ depth gates](fetch-completeness-and-depth-gates.md) — near-floor DEPTH_FAILED usually = stale/partial fetch; startFetchJob dedups onto zombie QUEUED jobs (check mi_fetch_jobs first).
- [Awareness depth cascade & AEL v2 fields](awareness-depth-cascade.md) — AEL v2 is snake_case with deepCause/cause item fields; consumers reading camelCase/description get rootCauses=0 SILENTLY → DEPTH_FAILED cascades all 15 engines. BuildPlanLayer takes depth-gate+AEL as caller params (never reloads DB); prod route trusts req.body (integrity gap).
- [BLOCKED_BY_PLATFORM self-suppression](blocked-by-platform-cooldown.md) — persisted 24h cooldown on the competitor row short-circuits fetches to status=BLOCKED (→ ALL_FAILED) without touching the network; fleet-wide same-minute blocks = spurious contention, not real platform blocks; forceRefresh=true bypasses to prove transport.
- [Apify fallback rung design](apify-fallback-rung.md) — slow fallback transports must be opt-in per call path (watchdog budgets); sanitize provider errors so block-detectors don't misread "403"; completed 0-post actor runs = healthy-empty variance.
- [P-2 competitor classification SSOT](p2-classification-ssot.md) — competitor_post_classifications is wired into Content DNA (BL-2) + MI multi-source signal pipeline; Watchtower and Pipeline corpus are known remaining gaps; narrative dim is intentionally UNKNOWN-heavy for Instagram food posts.
- [P-3 semantic intelligence upgrade](p3-semantic-intelligence.md) — Watchtower: 10 semantic event kinds (4 added P-3); market scope at promotion; confirmed events via /api/perception/market-signals + MarketSignalsCard. Free-text fields excluded from diffing (noise). Market-leader tier deferred (needs reliable follower data).
- [Performance-loop persist](performance-loop-persist.md) — report row = freeze; persist re-checks active truth under per-window advisory lock (TRUTH_CHANGED abort) or stale sales freeze; memory gate blocks <0.65 conf — record it, don't tune it.
- [Grounded AI interpretation layer](grounded-interpretation-layer.md) — judge prompts must whitelist signal-grouping language (grouping ≠ causation); big-bundle LLM calls need sized timeout/tokens + retry; window-align every source behind a "last N days" claim.
- [Memory architecture verdict](memory-architecture-audit.md) — five-layer model (obs→events→outcomes→beliefs→interpretations); additive-only evolution; merges/graph/embeddings rejected; ci_competitors is multi-writer.
- [Strategic reasoning evidence registry](strategic-reasoning-layer.md) — citable-ref registry as the grounding contract; absence itself must be citable evidence; market-memory history compares same-window rows on windowTo; tenant-scope every new read path + cache.
- [Evidence lineage immutability](evidence-lineage-immutability.md) — citation registries over mutable rows need content-versioned UIDs + append-only inserts; UID-stable upsert-refresh rewrites history behind old runs' citations.
- [Watchtower/boss trigger gating](watchtower-trigger-gating.md) — boss-run-gated layers (Watchtower, change events) are silently dark for campaigns without an APPROVED plan; check trigger gating before suspecting detection logic.
- [Live-campaign production flow](live-campaign-production-flow.md) — validate via real routes + JWT minted with the server's signing config; scheduler tick fires 60s post-boot; restarts strand ACTIVATING plans (no recovery sweep).
- [Plan gates are advisory](live-campaign-production-flow.md) — plans reach APPROVED despite BLOCKED_BY_INTEGRITY v2 runs and critical validation contradictions; uncertainty guard sees only section outputs, never the integrity verdict.
- [BLOCKED_BY_INTEGRITY root cause](blocked-by-integrity-root-cause.md) — cause is AEL absent-evidence cascade (0.00 confidence floor → SGL blocks 7 engines → Integrity gets null inputs), not engine timeouts; timeout increase has no effect on a synchronous integrity check receiving null snapshots.
- [Persistent market beliefs verdict](persistent-market-beliefs.md) — adopt carry-forward of validated audience signals across quiet windows (Unknown ≠ No Change); approved signals live on strategy_roots not strategic_plans; STABLE requires healthy observation window.
- [SQL migrations, not db:push](sql-migrations-convention.md) — drizzle push hangs on a no-TTY rename prompt; write server/migrations/sql/NNN_*.sql + bump REQUIRED_SCHEMA_VERSION + npm run db:migrate; boot log confirms schema floor.
- [Audience confidence forensics & v2](audience-confidence-forensics.md) — decompose confidence arithmetically before blaming inputs; v2 model scores against collectable sources + evidencing competitors; breakdown.finalConfidence ≠ emitted score (post-hoc scaling/boosts).
- [Watchtower seed cleanup](watchtower-seed-cleanup.md) — 28 synthetic candidates + 22 orphan snapshots deleted 2026-08-07; manifest in .local/validation/; dummy-run test row and one dangling-ref confirmed event deliberately left.
- [V2 pipeline production state](v2-pipeline-production-state.md) — only 2 real campaigns exist; C1 (burger/Dubai) runs 13/15 engines + strategy_root written; C2 (forma/Istanbul) correctly BLOCKED (weak data); Iteration stops by design awaiting primaryKpi/dataWindowDays from user.
- [Performance loop truthfulness](performance-loop-truthfulness.md) — SUCCESS-only scrape coverage; outcomes strictly comparator-driven (no legacy fallback); freeze the exact computed result, never recompute at persist; client trust DTO mirrors persisted JSON names.
