- [Avyron verification & tsc workflow](avyron-verification-workflow.md) — run tsc alone (chained greps time out & truncate it); judge by net-new errors vs the large baseline; verify orchestrator behaviour with focused tsx harnesses driving the real units, not a full DB+proxy run.
- [Avyron deploy/boot failure modes](avyron-deploy-boot.md) — esbuild bundles unimported identifiers (crash at boot, not build); global /api authMiddleware gate means no per-route auth; prod is verify-only so prod DB must be migrated to REQUIRED_SCHEMA_VERSION independently.
- [DrizzleQueryError .cause shape](drizzle-error-cause-shape.md) — pg detail lives in e.cause.message not e.message; catch/idempotency guards matching e.message silently break after a Drizzle bump.
- [Positioning gate coherence](positioning-gate-coherence.md) — composer prompt, grounding allowlist, and judge anchor must change in lockstep or the engine doom-loops; judge rejection with generic DNA is truthful degradation, fix via richer DNA not gate tuning.
- [MI snapshot resolution](mi-snapshot-resolution.md) — MI snapshots rarely carry orch runIds; run-pinned lookups starve reuse_allowed surfaces. Phantom drizzle column ⇒ "Cannot convert undefined or null to object".
- [Persisted UI-state hydration race](persisted-state-hydration-race.md) — async server hydration resets local UI state and clobbers mount-time deep links; gate the reset behind an unconsumed-intent ref and e2e-test the cold path.
- [Bright Data Unlocker quirks](brightdata-unlocker-quirks.md) — errors ride on HTTP 200 headers; Google raw HTML permanently off (review texts need SERP zone); zone IP allowlists break Replit; fingerprint secrets to detect identical re-pastes.
- [Retry paths drop context threading](retry-path-context-threading.md) — regenerate/retry call sites silently omit args the attempt-1 call passes; audit every retry branch and verify per-attempt evidence logs.
- [Shared-table row classes](shared-table-row-classes.md) — new row class in a shared table: fence EVERY reader; open-state unique indexes need expiry closure; two-fetch confirmation diffs vs stored baseline.
- [performance_snapshots row shapes](performance-snapshots-checkpoint-rows.md) — sync rows carry economics, revisit checkpoint rows are explicit-null; readers must filter by checkpoint or fabricate zeros; dedupe = DB partial unique index.
- [Engine result field-drift](engine-field-drift.md) — consumer reads of fields engines never emit yield undefined → gates see 0/empty and fail truthfully on healthy content; dump the actual evaluated input first, fix the read path never the gate.
- [_provenance key ownership](provenance-key-ownership.md) — engine-result `_provenance` belongs to the snapshot-reuse trust layer only; other metadata needs its own key or fresh outputs classify as REUSED_UNVERIFIED.
- [Funnel depth gate vs grounding](funnel-depth-gate-vs-grounding.md) — GROUNDING_CONTRACT_MET ≠ depth PASS: gate scores the deterministic funnel structure (no AEL refs), not the LLM's grounded output; verify per-attempt scored input empirically before trusting a scoring-input diagnosis.
- [Grounding contract verification](grounding-contract-verification.md) — groundingRefs are logged not persisted; ael=null ⇒ silent MET; grep -c over run log over-counts markers (use SUMMARY.json markerCounts).
- [DNA Enrichment Gate behaviour](dna-enrichment-gate-behavior.md) — Path A candidate re-passes the UNCHANGED judge (truthful reject is correct) → Path B flag; judge by the gate loop closing, not engine count — a richer anchor can reach FEWER engines yet still be BLOCKED.
- [Fetch completeness ⇄ depth gates](fetch-completeness-and-depth-gates.md) — near-floor DEPTH_FAILED usually = stale/partial fetch; startFetchJob dedups onto zombie QUEUED jobs (check mi_fetch_jobs first).
- [Awareness depth cascade & AEL v2 fields](awareness-depth-cascade.md) — AEL v2 is snake_case with deepCause/cause item fields; consumers reading camelCase/description get rootCauses=0 SILENTLY → DEPTH_FAILED cascades all 15 engines. BuildPlanLayer takes depth-gate+AEL as caller params (never reloads DB); prod route trusts req.body (integrity gap).
- [BLOCKED_BY_PLATFORM self-suppression](blocked-by-platform-cooldown.md) — persisted 24h cooldown on the competitor row short-circuits fetches to status=BLOCKED (→ ALL_FAILED) without touching the network; fleet-wide same-minute blocks = spurious contention, not real platform blocks; forceRefresh=true bypasses to prove transport.
