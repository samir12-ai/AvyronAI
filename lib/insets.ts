// F5.7 — centralized web-platform inset constants. Native platforms
// derive insets from useSafeAreaInsets(); these constants are only used
// when Platform.OS === "web".
export const WEB_TOP_INSET = 67;
export const WEB_BOTTOM_INSET = 34;
